
all:
  children:
    nginx:
      hosts:
        team-earth-nginx:
    worker:
      hosts:
        team-earth-worker:
    manager:
      hosts:
        team-earth-man:
  vars:
    ansible_user: zake3
    nsible_ssh_private_key_file: '~/.ssh/id_rsa'